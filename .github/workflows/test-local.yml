name: Test Local Action

on:
  pull_request:

jobs:
  check:
    name: Test Local Seacrest Action
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
        with:
          submodules: recursive

      - name: Run Seacrest
        uses: ./
        with:
          ethereum: https://goerli.infura.io

      - name: Check Account
        run: |
          echo "Checking account"
          curl -i http://localhost:8585 -X POST -H "Content-Type: application/json" --data '{"jsonrpc":"2.0","method":"eth_accounts","params":[],"id":67}'
          echo "Test Over"
